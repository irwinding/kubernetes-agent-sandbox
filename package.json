{
  "name": "langchain-kubernetes-agent-sandbox-devtools",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only tooling (husky + commitlint) to enforce conventional commits for semantic-release.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
